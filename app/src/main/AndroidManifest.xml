<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainAppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KopiJiwa"
        tools:targetApi="31">
        <activity
            android:name=".presentation.user.order.OrderFinishActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.history.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.user.product.DetailProductActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.user.order.OrderActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.user.product.ListProductActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.user.store.StoreActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.user.home.HomeActivity"
            android:exported="false" />

        <!--        start activity at splash-->
        <activity
            android:name=".presentation.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.outlet.add.EditOutletActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>